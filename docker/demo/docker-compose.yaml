name: foxy-img-proxy-demo
services:
    foxy:
        image: foxy-img-proxy-demo:0.9.4
        build:
            context: ../..
            dockerfile: Dockerfile-Demo
        ports:
            - "8080:8080"
        environment:
            MAX_CONNECTIONS: 16