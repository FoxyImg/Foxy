name: foxy-img-proxy-demo
services:
    foxy:
        image: foxy-img-proxy-demo:0.9.0
        build:
            context: ../..
            dockerfile: Dockerfile-Demo
        volumes:
            - ./demo.env:/.env:ro
        ports:
            - "8080:8080"